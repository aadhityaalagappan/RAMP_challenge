pipeline {
  agent any 

  stages {
    stage("build") {
      steps {
        echo "Build stage done"
      }
    }

    stage("test") {
      steps {
        echo "Test stage done"
      }
    }

    stage("deploy") {
      steps {
        echo "Deploy stage done"
      }
    }
  }
}
